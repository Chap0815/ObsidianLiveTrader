# Direct dev-only tooling — NOT installed at runtime.
# The exact transitive dev closure lives in requirements-dev.lock. CI installs
# requirements.lock + requirements-dev.lock with --no-deps for reproducibility.
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.15.22
pip-audit==2.10.1
# Starlette TestClient backend; keeps the deprecated httpx fallback out of tests.
httpx2==2.12.0
# Optional offline UI regression runner: scripts/ui_smoke.py
playwright==1.62.0
